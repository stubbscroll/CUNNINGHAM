cunningham chains!

some small programs to find cunningham chains, each version will improve upon
the previous one

versions so far:

01 - find chains by stupid brute force
02 - will use a simple sieve

warning, external dependencies: gmp (gnu multiprecision library)
